package com.example.sample

class NumberUtils {
    fun isEvenNumber(number: Int): Boolean {
        return number % 2 == 0
    }

    fun isOdd(number: Int): Boolean {
        return number % 2 != 0
    }
}